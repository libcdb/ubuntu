/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-powerpc)
GROUP ( /usr/powerpc-linux-gnu/lib/libc.so.6 /usr/powerpc-linux-gnu/lib/libc_nonshared.a  AS_NEEDED ( /usr/powerpc-linux-gnu/lib/ld.so.1 ) )
